datos = [2, 4, 6, 8, 10, 12]  # Crea una lista con los valores 2, 4, 6, 8, 10, 12

for x in range(len(datos)):  # Itera sobre los índices de la lista 'datos'
    print('incisos', x, 'valores', datos[x], 'al cuadrado = ', datos[x] * datos[x])  
    # Imprime el índice (inciso), el valor correspondiente de la lista, 
    # y el valor al cuadrado (datos[x] * datos[x])
